worker_threads: 8
timeout: 10
ping_on_rotate: True

file_roots:
  base:
    - /srv/salt/base

pillar_roots:
  base:
    - /srv/pillar/base

log_level_logfile: info

file_ignore_regex:

  - '/\.git($|/)'

